# deps
node_modules/
**/node_modules/

# next
.next/
out/
next-env.d.ts

# prisma / sqlite (local data — never commit)
*.db
*.db-journal
*.db-wal
*.db-shm
**/data/

# quick-capture image uploads (local data — never commit)
cockpit/uploads/
uploads/

# project packs — local/private/customer data; never commit
cockpit/projects/

# env & secrets
.env
.env.local
*.local

# os / editor
.DS_Store
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
*.log
.idea/
.vscode/

# local agent/harness state (scheduled-task locks, etc.) — never commit
.claude/

# agent session handoff docs — never commit
HANDOFF*
PRODUCTION-READINESS*

# build
dist/
build/

# test artifacts
playwright-report/
test-results/
.playwright-mcp/

# local screenshot / verification scratch — never commit root-level image binaries
/*.png
/*.jpg
/*.jpeg

# typescript incremental build info
*.tsbuildinfo
